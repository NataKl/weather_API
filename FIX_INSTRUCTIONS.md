# 🔧 Исправление проблемы с кнопками в Telegram боте

## 📋 Что было не так?

В коде Telegram-бота кнопки создавались, но не отображались в интерфейсе Telegram из-за отсутствия параметра `is_persistent=True` в `ReplyKeyboardMarkup`.

## ✅ Что было исправлено?

### 1. **Добавлен параметр `is_persistent=True` в функции клавиатур**

**До:**
```python
markup = types.ReplyKeyboardMarkup(
    resize_keyboard=True,
    one_time_keyboard=False
)
```

**После:**
```python
markup = types.ReplyKeyboardMarkup(
    resize_keyboard=True,
    one_time_keyboard=False,
    is_persistent=True  # ← Добавлено!
)
```

### 2. **Исправленные функции:**
- `get_main_menu()` - главное меню с кнопками
- `get_back_menu()` - кнопка возврата
- `location_command()` - кнопка для отправки местоположения

## 🎯 Что делает `is_persistent=True`?

Этот параметр гарантирует, что клавиатура:
- Остаётся видимой постоянно
- Не исчезает после отправки сообщения
- Корректно отображается в разных версиях Telegram

## ⚙️ Важно! Настройка переменных окружения

Для работы бота необходим файл `.env` с токенами:

1. Создайте файл `.env` в корневой папке проекта
2. Скопируйте содержимое из `env_template.txt`
3. Замените значения на реальные:
   - `BOT_TOKEN` - получите от @BotFather в Telegram
   - `API_KEY` - получите на https://openweathermap.org/api

## 🚀 Как запустить бота после исправлений?

### Вариант 1: Через терминал (рекомендуется)
```bash
# 1. Активируйте виртуальное окружение
.venv\Scripts\activate

# 2. Запустите бота
python bot.py
```

### Вариант 2: Через bat-файл
Просто дважды кликните на файл `start_bot.bat`

## 🧪 Проверка работы

После запуска бота:
1. Откройте Telegram
2. Найдите своего бота
3. Отправьте команду `/start`
4. Вы должны увидеть **7 кнопок**:
   - 🏙️ Погода в городе
   - 📅 Прогноз на 5 дней
   - 📍 Моё местоположение
   - 🔔 Уведомления
   - ⚖️ Сравнить города
   - 📊 Расширенные данные
   - ❓ Помощь

## 🔍 Если кнопки всё ещё не появляются:

1. **Перезапустите бота:**
   - Остановите: Ctrl+C в терминале
   - Запустите снова: `python bot.py`

2. **Проверьте файл .env:**
   - Убедитесь, что файл существует
   - Проверьте правильность токенов

3. **В Telegram:**
   - Попробуйте команду `/menu` для принудительного показа кнопок
   - Проверьте, не скрыта ли клавиатура (нажмите на иконку клавиатуры внизу экрана)

4. **Обновите библиотеку:**
   ```bash
   pip install --upgrade pyTelegramBotAPI
   ```

## 📱 Почему это важно?

Параметр `is_persistent` был добавлен в новых версиях Telegram Bot API для улучшения пользовательского опыта. Без него:
- Клавиатура может исчезать
- Кнопки могут не отображаться на некоторых устройствах
- Пользователю придётся вручную открывать клавиатуру

---

**Дата исправления:** 23.01.2026  
**Версия:** 1.0
